plugins {
    id "base"
}

description = "HTML module placeholder. Legacy GWT build is disabled pending modernization."

tasks.register("htmlMigrationPending") {
    group = "help"
    doLast {
        println("HTML/GWT build is disabled. Migrate separately if web target is needed.")
    }
}
