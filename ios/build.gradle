plugins {
    id "base"
}

description = "iOS module placeholder. Legacy RoboVM build is disabled pending modernization."

tasks.register("iosMigrationPending") {
    group = "help"
    doLast {
        println("iOS build is disabled. Migrate separately (e.g., MobiVM) if iOS target is needed.")
    }
}
